import { LoadItDirective } from './load-it.directive';

describe('LoadItDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadItDirective();
    expect(directive).toBeTruthy();
  });
});
